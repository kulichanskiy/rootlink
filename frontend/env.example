# Proxy target for Next.js rewrites (used only by next dev / next build)
# Browser calls http://localhost:3000/api/... → forwarded to Spring Boot.
BACKEND_URL=http://localhost:8080

# Optional: skip the proxy and call Spring directly from the browser (CORS must allow your origin)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api
