# Used by next.config.ts rewrites: browser calls /api/* → Spring Boot.
BACKEND_URL=http://localhost:8080

# Optional: set only if you want the browser to talk to the API host directly
# (bypasses the Next proxy). Must include /api suffix.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api
